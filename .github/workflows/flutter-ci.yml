# .github/workflows/flutter-ci.yml
name: Flutter CI

on: [push]
  #push:
  #  branches: [ main, develop ]
  #pull_request:
  #  branches: [ main, develop ]

jobs:
  #test:
  #  runs-on: ubuntu-latest

  #  steps:
  #    - name: Checkout code
  #      uses: actions/checkout@v4

  #    - name: Setup Flutter
  #      uses: subosito/flutter-action@v2
  #      with:
  #        flutter-version: '3.35.7' # Especifica tu versión de Flutter

  #    - name: Install dependencies
  #      run: flutter pub get

  #    - name: Analyze code
  #      run: flutter analyze

  #    - name: Run tests
  #      run: flutter test

  #    - name: Check formatting
  #      run: flutter format --set-exit-if-changed .

  build:
    #needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.1'

      - name: Install dependencies
        run: flutter pub get

      - name: Build web
        run: flutter build web --release

      - name: Build apk
        run: flutter build apk --release